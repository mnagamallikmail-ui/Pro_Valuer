import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds & Surfaces (Luminous & Crisp)
  static const Color background = Color(0xFFFFFFFF);     // Pristine Pure White
  static const Color surface = Color(0xFFF8F9FA);        // Ultra-Light Luminous Gray
  static const Color white = Color(0xFFFFFFFF);         // Mandatory for charts/reports

  // Primary Text, Headers & Icons (Anchor)
  static const Color primaryText = Color(0xFF2B3A42);    // Deep Crisp Slate
  static const Color textPrimary = Color(0xFF2B3A42);    // Alias for compatibility

  // Secondary Elements & Muted Text
  static const Color textSecondary = Color(0xFF718096);  // Cool Slate Gray
  static const Color secondaryText = Color(0xFF718096);  // Alias
  static const Color muted = Color(0xFF718096);          // Alias
  
  // Primary Accents (Teal)
  static const Color primary = Color(0xFF38B2AC);        // Vibrant Teal
  static const Color accent = Color(0xFF38B2AC);         // Alias

  // Action Buttons (Clay)
  static const Color action = Color(0xFFE27D60);         // Warm Clay
  static const Color primaryAction = Color(0xFFE27D60);  // Alias
  
  // Semantic & Borders
  static const Color border = Color(0xFFE2E8F0);         // Luminous Border Gray
  static const Color error = Color(0xFFF56565);          // Crisp Red
  static const Color success = Color(0xFF48BB78);        // Crisp Green
  static const Color warning = Color(0xFFECC94B);        // Crisp Amber
  static const Color secondary = Color(0xFF718096);      
}

