import "package:flutter/material.dart";

class AppColors {
  // Backgrounds & Surfaces
  static const Color background = Color(0xFFFFFFFF);     // Pure White
  static const Color surface = Color(0xFFF2F5FF);        // Ghost White

  // Primary Text, Headers & Icons
  static const Color primaryText = Color(0xFF55D6C2);    // Turquoise
  static const Color textPrimary = Color(0xFF55D6C2);    // Alias for compatibility

  // Brand / Primary Action
  static const Color primary = Color(0xFFF49097);        // Salmon Pink

  // Highlights
  static const Color secondary = Color(0xFFF5E960);      // Corn Yellow

  // Subtle Elements
  static const Color accent = Color(0xFFDFB2F4);         // Lilac
  static const Color textSecondary = Color(0xFFDFB2F4);  // Alias for compatibility
  static const Color border = Color(0xFFDFB2F4);         // Lilac

  // Semantic
  static const Color error = Color(0xFFF49097);
  static const Color success = Color(0xFF55D6C2);
  static const Color warning = Color(0xFFF5E960);
}
